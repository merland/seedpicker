{
  "name": "seedpicker",
  "version": "1.0.0",
  "description": "Contains a GUIDE and a CALCULATOR to help you safely create a private key.",
  "main": "index.js",
  "dependencies": {
    "bip39": "^2.5.0",
    "browserify": "^16.2.3"
  },
  "devDependencies": {},
  "scripts": {
    "run": "browserify -r bip39 -o calculator/bundle.js",
    "build": "browserify -r bip39 -o calculator/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "seedpicker@megabit.se",
  "license": "MIT"
}
